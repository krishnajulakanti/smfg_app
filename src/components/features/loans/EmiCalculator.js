import React from 'react';
import { View, Text, StyleSheet, FlatList, TouchableOpacity, ScrollView } from 'react-native';

const EmiCalculator = () => {
    return (
        <Text>Emi Calculator Screen</Text>
    )
};

export default EmiCalculator;